<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="LicznikZnaków.MainPage">

    <StackLayout>
        <Label Text="Główna strona"/>
        <Entry Text="{Binding NormalText}" Placeholder="Wprowadź Tekst"/>
        <Label Text="{Binding NormalTextLength}"/>
        <Label Text="Tekst ma więcej niż 4 znaki!" IsVisible="{Binding NormalTextVisibility}"/>
        <Button Text="Oblicz!" Command="{Binding DoSpellCommand}"/>
        <Label Text="{Binding Result}"/>
        <Button Text="Next Page" Command="{Binding NextPageCommand}"/>
    </StackLayout>

</ContentPage>
